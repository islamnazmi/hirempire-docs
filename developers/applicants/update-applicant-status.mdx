---
title: "Update applicant status"
api: "PATCH https://api.hirempire.com/v1/update-applicant"
description: "Update the status of an existing applicant in your Hirempire account"
---

## Request Body

<ParamField body="applicant_id" type="string" required placeholder="Enter applicant ID">
  The unique identifier of the applicant to update
</ParamField>

<ParamField body="status" type="string" required placeholder="Enter status">
  The new status for the applicant. Valid values: Applied, Reviewed, Accepted, Scheduled, Met, Hired, Rejected
</ParamField>

## Authentication

<ResponseField name="Authorization" type="string" required>
Bearer authentication header of the form `Bearer <token>`, where `<token>` is your auth token. 
</ResponseField>

<RequestExample>

```bash cURL
curl --location --request PATCH 'https://api.hirempire.com/v1/update-applicant' \
--header 'Authorization: Bearer YOUR_TOKEN' \
--header 'Content-Type: application/json' \
--data-raw '{
    "applicant_id": "xyz456abc123",
    "status": "Rejected"
}'
```

```javascript JavaScript
const response = await fetch('https://api.hirempire.com/v1/update-applicant', {
  method: 'PATCH',
  headers: {
    'Authorization': 'Bearer YOUR_TOKEN',
    'Content-Type': 'application/json'
  },
  body: JSON.stringify({
    applicant_id: "xyz456abc123",
    status: "Rejected"
  })
});

const data = await response.json();
console.log(data);
```

```python Python
import requests
import json

url = "https://api.hirempire.com/v1/update-applicant"
headers = {
    "Authorization": "Bearer YOUR_TOKEN",
    "Content-Type": "application/json"
}

payload = {
    "applicant_id": "xyz456abc123",
    "status": "Rejected"
}

response = requests.patch(url, headers=headers, data=json.dumps(payload))
data = response.json()
print(data)
```

```php PHP
<?php
$curl = curl_init();

$payload = json_encode([
    "applicant_id" => "xyz456abc123",
    "status" => "Rejected"
]);

curl_setopt_array($curl, array(
  CURLOPT_URL => 'https://api.hirempire.com/v1/update-applicant',
  CURLOPT_RETURNTRANSFER => true,
  CURLOPT_CUSTOMREQUEST => 'PATCH',
  CURLOPT_POSTFIELDS => $payload,
  CURLOPT_HTTPHEADER => array(
    'Authorization: Bearer YOUR_TOKEN',
    'Content-Type: application/json'
  ),
));

$response = curl_exec($curl);
curl_close($curl);

$data = json_decode($response, true);
print_r($data);
?>
```

```ruby Ruby
require 'net/http'
require 'json'

uri = URI('https://api.hirempire.com/v1/update-applicant')
http = Net::HTTP.new(uri.host, uri.port)
http.use_ssl = true

request = Net::HTTP::Patch.new(uri)
request['Authorization'] = 'Bearer YOUR_TOKEN'
request['Content-Type'] = 'application/json'
request.body = {
  applicant_id: "xyz456abc123",
  status: "Rejected"
}.to_json

response = http.request(request)
data = JSON.parse(response.body)
puts data
```

```go Go
package main

import (
    "bytes"
    "encoding/json"
    "fmt"
    "io"
    "net/http"
)

func main() {
    payload := map[string]interface{}{
        "applicant_id": "xyz456abc123",
        "status": "Rejected",
    }
    
    jsonPayload, _ := json.Marshal(payload)
    
    client := &http.Client{}
    req, _ := http.NewRequest("PATCH", "https://api.hirempire.com/v1/update-applicant", bytes.NewBuffer(jsonPayload))
    req.Header.Add("Authorization", "Bearer YOUR_TOKEN")
    req.Header.Add("Content-Type", "application/json")
    
    resp, _ := client.Do(req)
    defer resp.Body.Close()
    
    body, _ := io.ReadAll(resp.Body)
    fmt.Println(string(body))
}
```

```java Java
import java.net.http.HttpClient;
import java.net.http.HttpRequest;
import java.net.http.HttpResponse;
import java.net.URI;

public class HirempireAPI {
    public static void main(String[] args) throws Exception {
        String payload = """
        {
            "applicant_id": "xyz456abc123",
            "status": "Rejected"
        }
        """;
        
        HttpClient client = HttpClient.newHttpClient();
        HttpRequest request = HttpRequest.newBuilder()
            .uri(URI.create("https://api.hirempire.com/v1/update-applicant"))
            .header("Authorization", "Bearer YOUR_TOKEN")
            .header("Content-Type", "application/json")
            .method("PATCH", HttpRequest.BodyPublishers.ofString(payload))
            .build();
            
        HttpResponse<String> response = client.send(request, 
            HttpResponse.BodyHandlers.ofString());
        System.out.println(response.body());
    }
}
```

```csharp C#
using System;
using System.Net.Http;
using System.Text;
using System.Threading.Tasks;

class Program
{
    private static readonly HttpClient client = new HttpClient();

    static async Task Main(string[] args)
    {
        client.DefaultRequestHeaders.Add("Authorization", "Bearer YOUR_TOKEN");
        
        var payload = """
        {
            "applicant_id": "xyz456abc123",
            "status": "Rejected"
        }
        """;
        
        var content = new StringContent(payload, Encoding.UTF8, "application/json");
        HttpResponseMessage response = await client.PatchAsync("https://api.hirempire.com/v1/update-applicant", content);
        string responseBody = await response.Content.ReadAsStringAsync();
        
        Console.WriteLine(responseBody);
    }
}
```

```swift Swift
import Foundation

let url = URL(string: "https://api.hirempire.com/v1/update-applicant")!
var request = URLRequest(url: url)
request.httpMethod = "PATCH"
request.setValue("Bearer YOUR_TOKEN", forHTTPHeaderField: "Authorization")
request.setValue("application/json", forHTTPHeaderField: "Content-Type")

let payload: [String: Any] = [
    "applicant_id": "xyz456abc123",
    "status": "Rejected"
]

request.httpBody = try? JSONSerialization.data(withJSONObject: payload)

let task = URLSession.shared.dataTask(with: request) { data, response, error in
    if let data = data {
        let json = try? JSONSerialization.jsonObject(with: data)
        print(json ?? "No data")
    }
}

task.resume()
```

```kotlin Kotlin
import kotlinx.coroutines.*
import java.net.http.HttpClient
import java.net.http.HttpRequest
import java.net.http.HttpResponse
import java.net.URI

fun main() = runBlocking {
    val payload = """
    {
        "applicant_id": "xyz456abc123",
        "status": "Rejected"
    }
    """.trimIndent()
    
    val client = HttpClient.newHttpClient()
    val request = HttpRequest.newBuilder()
        .uri(URI.create("https://api.hirempire.com/v1/update-applicant"))
        .header("Authorization", "Bearer YOUR_TOKEN")
        .header("Content-Type", "application/json")
        .method("PATCH", HttpRequest.BodyPublishers.ofString(payload))
        .build()
        
    val response = client.send(request, HttpResponse.BodyHandlers.ofString())
    println(response.body())
}
```

```rust Rust
use reqwest::header::{HeaderMap, HeaderValue, AUTHORIZATION, CONTENT_TYPE};
use serde_json::json;

#[tokio::main]
async fn main() -> Result<(), Box<dyn std::error::Error>> {
    let mut headers = HeaderMap::new();
    headers.insert(AUTHORIZATION, HeaderValue::from_str("Bearer YOUR_TOKEN")?);
    headers.insert(CONTENT_TYPE, HeaderValue::from_str("application/json")?);
    
    let payload = json!({
        "applicant_id": "xyz456abc123",
        "status": "Rejected"
    });
    
    let client = reqwest::Client::new();
    let response = client
        .patch("https://api.hirempire.com/v1/update-applicant")
        .headers(headers)
        .json(&payload)
        .send()
        .await?;
        
    let body = response.text().await?;
    println!("{}", body);
    
    Ok(())
}
```

</RequestExample>

## Response

<ResponseField name="success" type="boolean">
Indicates if the applicant status was updated successfully
</ResponseField>

<ResponseField name="id" type="string">
The unique identifier of the updated applicant
</ResponseField>

<ResponseField name="status" type="string">
The new status of the applicant
</ResponseField>

<ResponseExample>
```json
{
  "success": true,
  "id": "xyz456abc123",
  "status": "Rejected"
}
```
</ResponseExample>